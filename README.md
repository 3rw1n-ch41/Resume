# Erwin's resume

This repository holds Erwin's resume and aims to provide a version controlled environment for Erwin's resume. The overall resume is rendered in python using the `renderCV` package

## Version History

* 0.1
    * Initial Release

## Folder structure
